import { LogProvider } from "../../logging";
import { ProxyConnectionChangedHandler, ProxyConnectionEvent, ProxyConnectionStatus } from "./types";
export declare class ProxyConnectionStatusManager {
    private readonly changeHandlers;
    private readonly logger;
    private status;
    constructor(provider: LogProvider);
    getStatus(): ProxyConnectionStatus;
    update(evt: ProxyConnectionEvent): void;
    onChange(handler: ProxyConnectionChangedHandler): void;
    offChange(handler: ProxyConnectionChangedHandler): void;
}
//# sourceMappingURL=proxy-connection-status-manager.d.ts.map