import { LogProvider } from "../logging";
import { RequestMessage, ResponseMessage } from "../messaging";
import { ConnectResponseData } from "./request-handlers";
export declare class RequestManager {
    private readonly requestMap;
    private readonly logger;
    constructor(provider: LogProvider);
    processRequest<TResponse extends ConnectResponseData>(request: RequestMessage): Promise<TResponse>;
    processResponse(response: ResponseMessage): void;
    private handleTimeout;
}
//# sourceMappingURL=request-manager.d.ts.map